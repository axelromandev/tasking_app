import 'package:flutter/material.dart';

class ListTasksUpdateDialog extends StatelessWidget {
  const ListTasksUpdateDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
